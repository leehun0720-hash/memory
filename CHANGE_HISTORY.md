# 변경 이력 · 다음 에이전트 인수인계

이 문서는 사용자의 요청으로 프로젝트 루트에 저장합니다. 다음 작업을 시작할 때 이 문서와 `docs/ALIMTALK.md`를 먼저 읽고, 실제 코드와 Git 상태를 확인해 주세요. 자격 증명이나 초대 토큰은 이 문서에 적지 않습니다.

## 2026-09-21 — 참고 이미지 기반 따뜻한 디자인 개편

사용자가 제공한 추모 포스터, 꽃 장식, 아치형 인물 사진, 불교 향·연꽃, 기독교 십자가와 햇빛 이미지를 분위기와 배치의 참고로 삼아 가족 앱을 개편했습니다.

- 네 테마 전용 이미지 신규 생성: 전통 흰 국화/올리브, 불교 연꽃/촛불/호박빛, 천주교 백합/성당/청회빛, 기독교 십자가/햇살/세이지빛. 참고 이미지의 인물과 문구는 사용하지 않았으며 기존 등록 사진은 그대로 유지합니다.
- 참배 첫 화면에 따뜻한 환영 표지, 추모 화면에 종교별 이미지 표지와 아치형 사진, 이름·생몰일·추모 문구를 배치했습니다. 실제 카메라 영상은 별도 화면에 유지합니다.
- 본문과 버튼은 Noto Sans KR 17px, 제목은 28~38px. 전통 Noto Serif KR, 불교 고운바탕, 천주교 나눔명조, 기독교 고운돋움으로 제목을 구분합니다. 보조 설명 13~15px(좁은 테마 카드 설명 12px), 충분한 행간과 44px 이상 터치 영역을 사용합니다.
- 설정의 종교 선택을 이미지 카드로 변경하고, 선택한 테마의 이미지·글꼴·색상을 함께 미리 봅니다. 명시적인 적용 버튼과 가족별 저장 권한은 유지합니다.
- 설정은 제목 → 테마 꾸미기 → 가족 기록/알림 순서로 정리했습니다. 바로가기는 화면 제목/표지 다음에 배치합니다. 하단 ‘제사’ 메뉴를 ‘일정’으로 변경했습니다(기존 라우트는 ritual 유지).
- 쉬운 화면의 시스템 글꼴, 큰 글씨, 장식 최소화와 움직임 감소를 유지합니다. 가족별 자료·알림·AI·카메라 API 정책 변경은 없습니다.

변경 파일: `server/static/css/warm.css`(가족 앱 전용), `server/static/js/app.js`, `community.js`, `server/static/index.html`, `server/main.py`(CSS 캐시 갱신 목록), 신규 PNG 이미지 4장, `docs/WARM_DESIGN.md`.

이미지는 내장 image_gen으로 생성하여 `server/static/images/themes/*-garden.png`에 복사했습니다. 원본 생성 프롬프트와 디자인 기준은 `docs/WARM_DESIGN.md`에 보관했습니다. 파일은 로컬 정적 자산이며 종교의 공식 색상을 주장하지 않습니다.

검증: 기존 전체 테스트 **45 passed in 6.43s**, JavaScript 문법 검사 통과. 데스크톱 설정/추모와 390px 모바일 설정/추모/참배를 시각 검토했으며 가로 넘침이 없습니다. 네 가지 테마 미리보기의 이미지와 서체 전환을 확인했습니다. 검증 중 가족의 저장된 종교 테마는 변경하지 않았습니다. 최종 코드를 로컬 8765 서버에 반영했습니다.

작업 브랜치: `codex/family-community`. 이전 기능/알림톡의 미설정 항목은 아래 인수인계 기록을 그대로 참고하세요.

## 2026-09-21 — 가족 기록·일정·추모 경험 확장

### 사용자 요청과 범위

- 심플하고 단정하며 기품 있는 UI/UX.
- 추모 공간의 종교별 테마 적용, 여백 무늬와 글꼴 변경.
- 관리자 페이지 접속 아이콘, Git 업데이트.
- 제안한 추가 기능 8가지 모두 구현 승인.
- 외부 일정 알림 방식은 **카카오 알림톡**으로 선택.
- 완료 후 다른 에이전트가 이해할 수 있도록 변경 이력 파일 저장.

### 이전에 완료한 화면 작업

기존 커밋 `e6c6229`에서 가족 화면을 밝고 절제된 레이아웃으로 정리하고, 테마 미리보기/명시적인 적용 버튼, 전통·불교·천주교·기독교 무늬와 글꼴을 반영했습니다. 관리자 실행용 `관리자.bat`과 바탕화면 바로가기를 추가했습니다. 이 커밋은 원격 main에 업로드되어 있습니다.

### 이번에 추가한 기능

1. **가족 앨범**: 사진·영상·음성·이야기 등록, 날짜, 가족 공개/작성자와 계약자만 공개. 일반 가족의 글은 계약자 승인 후 가족에게 보입니다. 파일은 30MB 이하, 이미지는 위치 정보 제거와 방향 보정 후 JPEG로 보관합니다. 공개 승인과 AI 활용 동의는 독립적입니다.
2. **기일·가족 달력**: 양력/음력, 기일/생신/명절, 매년 반복, 윤달 없는 해의 평달 대체 또는 건너뛰기, 날짜 없는 해의 말일 또는 건너뛰기, 날짜 미리보기. 캘린더 파일은 향후 2년의 변환된 개별 일정입니다.
3. **쉬운 화면**: 참배하기·사진 보기·마음 남기기의 큰 버튼, 단순한 글꼴, 큰 글씨, 무늬와 움직임 최소화. 기기별 선택을 저장합니다.
4. **관리자 오늘 할 일**: 당일 의례, 공양 접수, 가족 승인 대기 자료, 오프라인 카메라, 24시간 이상 오래된 봉안함 사진. 가족 공개/AI 동의 결정 권한은 가족 계약자에게 유지합니다.
5. **자료 보관**: ZIP에 열람 가능한 자료 파일, 프로필·이야기·기존 자료 설명·방명록 JSON을 포함합니다. PDF 추억책에는 가족 앨범 사진·이야기와 방명록이 담깁니다. 작별 절차와 별개로 다운로드합니다.
6. **종교별 추모 순서**: 전통 헌화, 불교 연꽃·합장, 천주교 촛불·기도, 기독교 묵상·기도. 별도 지정 전에는 해당 고인의 테마를 기본값으로 사용합니다. 추모문은 계약자가 직접 입력한 문구만 사용합니다.
7. **함께 참배 약속**: 한 번의 양력 약속, 참석/불참, 가족 접속 표시. 참배·마음 남기기 화면에서도 접속을 유지합니다. 기존 봉안함 카메라 이용 규칙을 재사용하며 가족 간 영상 통화는 구현하지 않았습니다.
8. **초대·권한 관리**: 새 초대 기본 30일(1~365일), 재발급 시 이전 토큰 무효화, 즉시 접속 해제, 보기/대화 권한 변경. 계약자 계정은 이 화면에서 해제할 수 없습니다. 기존 초대는 자동 만료시키지 않습니다.

추가 연결: 승인된 가족 이야기 중 AI 사용에 별도로 동의한 자료만 대화 문맥에 포함합니다. 매 응답 전 권한·동의 상태를 다시 확인합니다. 승인 자료가 바뀌면 이전 응답 문맥을 초기화해 철회된 기록을 재사용하지 않게 합니다. ‘AI에 연결한 기록’은 참고 가능한 자료 목록이며 답변별 실제 인용 출처라고 표시하지 않습니다.

### 카카오 알림톡 구현 및 남은 외부 설정

- NAVER Cloud SENS 어댑터, HMAC 서명, 수신 동의, 전화번호, 7일 전/전날/당일, 알림 종류, 쉬는 기간, 중복 방지 DB 제약, 발송 대기열, 결과 조회를 구현했습니다.
- 한국 시간 09:00~19:59에 당일 생성된 대기 건만 발송합니다. 취소·초대 철회·불참·동의 철회를 전송 직전에 다시 확인합니다.
- `submitted`는 접수이고 `delivered`만 수신 성공입니다. 확실한 실패만 관리자 화면에서 수동 재시도합니다. 통신 결과 불명확/서버 재시작 중 발송은 `unknown`으로 남겨 자동 중복 전송을 막습니다.
- **실제 카카오 메시지는 이 작업에서 발송하지 않았습니다.** 카카오 채널, SENS 계정, 승인된 기본 텍스트 템플릿과 자격 증명이 필요합니다. 서비스 연결 전에는 앱 안 알림만 이용합니다.
- 환경변수 목록과 운영 순서는 `docs/ALIMTALK.md`, 빈 설정 예시는 `.env.example`에 있습니다. `.env`는 Git 제외입니다.
- SQLite를 공유하는 **단일 앱 프로세스, 상시 실행 서버**가 현재 운영 전제입니다. 서버리스 예약 실행/다중 워커 분산 발송은 이번 범위에 포함하지 않았습니다.

### 코드 위치

| 파일 | 역할 |
| --- | --- |
| `server/db.py` | 추가 테이블 7개와 인덱스, family_members 초대 만료·철회 컬럼 마이그레이션 |
| `server/community.py` | 계약 범위 권한, 음력 변환, 일정 계산, 알림 생성, 승인된 AI 자료 |
| `server/routers/community.py` | 앨범·일정·약속·알림 설정·초대 관리·추모문·ZIP/PDF API |
| `server/notifications.py` | 알림톡 발송/조회와 60초 주기 작업 |
| `server/keepsake.py` | 한글 글꼴을 포함하는 ReportLab 추억책 |
| `server/main.py` | 새 라우터, 알림 작업 시작/종료, 정적 파일 캐시 갱신 |
| `server/deps.py` | 만료/철회된 초대 인증 차단 |
| `server/routers/family.py` | 새 초대 유효기간 |
| `server/routers/chat.py` | 승인된 가족 기록 연결, 매 응답 권한/동의 재확인 |
| `server/routers/admin.py` | 오늘 할 일, 발송 상태, 실패 건 재요청 |
| `server/static/js/community.js` | 새 가족 화면과 입력 폼 |
| `server/static/css/community.css` | 앨범·모바일·쉬운 화면 스타일 |
| `server/static/js/app.js`, `index.html` | 기존 메뉴와 새 화면 연결 |
| `server/static/js/admin.js`, `admin.html` | 관리자 메뉴 및 작업 목록 |
| `tests/test_community.py` | 권한·파일·음력·초대·알림톡·내보내기 회귀 검증 |

추가 의존성: `korean-lunar-calendar>=0.4,<0.5`, `reportlab>=4,<5`. 실제 데이터 폴더는 `data/`이며 저장소에서 제외합니다. 스키마 변경은 앱 시작 시 추가 방식으로 수행되며 기존 테이블을 삭제하지 않습니다.

### 검증 기록

- 기존 테스트: 38개 통과.
- 새 테스트: 7개 통과. 다른 계약 자료 접근 차단, 승인 전/제한 공개 자료 숨김, AI 별도 동의, 파일 형식 검증, ZIP 권한 범위, PDF 생성, 음력/윤달/말일 변환, 약속 접근 권한, ICS 한국 시간→UTC, 알림 중복 방지, 초대 만료/재발급/철회, 수신 동의, 공급자 호출 모의 검증, 관리자 인증을 확인했습니다.
- 브라우저: 실제 로컬 설정 화면, 앨범 폼, 알림톡 설정 폼, 날짜 미리보기, 쉬운 화면, 모바일 추모 화면을 확인했습니다. 음력 2026-08-15 → 양력 2026-09-25 표시 확인.
- PDF: 가상 자료로 생성, 한글 텍스트 추출 확인, Poppler로 렌더링하여 여백과 한글 표시 확인.
- JavaScript: app.js/community.js/admin.js 문법 검사 통과.
- 실제 사용자 계정으로 기억/약속/동의를 새로 저장하거나 외부 AI·알림톡을 호출하는 검증은 하지 않았습니다. 저장·권한·발송 상태 변화는 격리 DB/API 테스트로 검증했습니다.

검증 명령:

```powershell
python -m pytest -q --basetemp=.tmp-community-final -p no:cacheprovider
node --check server/static/js/app.js
node --check server/static/js/community.js
node --check server/static/js/admin.js
```

Windows 샌드박스의 임시 폴더 접근 제한 때문에 pytest는 권한 승인 후 실행했습니다. `.tmp-*` 검증 산출물은 Git 제외입니다. 실제 알림톡 발송 테스트는 `provider_request`를 모의 처리하며 수행하지 않습니다.

### 다음 에이전트가 알아야 할 제한

- 음력 변환은 라이브러리가 지원하는 2050년까지입니다. 캘린더 파일은 구독이 아니므로 일정 변경 후 다시 내려받아야 합니다.
- ZIP은 파일 총합 250MB까지 메모리에서 생성합니다. 더 큰 자료는 개별 다운로드 또는 향후 스트리밍 내보내기가 필요합니다.
- PDF는 Windows 맑은 고딕/Linux NanumGothic 또는 `MEMORIAL_PDF_FONT`의 임베드 가능한 TTF를 사용합니다. 글꼴 없는 운영 환경에서는 별도 한글 TTF를 설치하고 렌더링을 확인하세요.
- 앨범 삭제는 목록과 AI 활용에서 제외하는 소프트 삭제입니다. 보관 파일의 영구 삭제/보존 기간 정책은 별도 운영 결정이 필요합니다.
- 기존 `view` 권한도 방명록을 남길 수 있었던 정책을 이어, 가족 기록/마음/약속 참여를 허용합니다. AI 대화는 `chat` 이상, 공개/AI 승인과 기념일 관리·초대 관리는 `manage`입니다.
- 앱은 FastAPI+SQLite+일반 JavaScript이며 npm 빌드가 필요하지 않습니다. 실행: `python -m uvicorn server.main:app --host 127.0.0.1 --port 8765 --no-access-log`.

### Git 및 실행 상태

이번 변경의 최종 테스트 결과와 Git 업로드 상태는 아래 완료 기록에서 확인합니다.

<!-- completion-record -->
- 최종 통합 테스트: **45 passed in 6.43s** (기존 38 + 신규 7).
- JavaScript 세 파일 문법 검사, Python compileall, Git diff 공백 검사 통과.
- 390px 모바일에서 쉬운 화면과 초대 관리 화면의 가로 넘침 없음. 검증 종료 시 브라우저 오류 로그 없음. 임시 화면 크기를 해제하고 일반 모드 설정 화면으로 복귀했습니다.
- 작업 브랜치: `codex/family-community` (기준: `main`의 `e6c6229`).
- 구현 커밋: `a99f5bd` — Add family memories, calendar, Alimtalk reminders and handoff notes.
- 원격 업로드 완료: `origin/codex/family-community`, GitHub `leehun0720-hash/memory`. main에는 이번 기능을 병합하지 않았습니다.
- 브랜치 주소: https://github.com/leehun0720-hash/memory/tree/codex/family-community
- 로컬 개발 서버를 최종 코드로 재시작했습니다. 접속 주소는 `http://127.0.0.1:8765/`입니다. 프로세스 ID나 임시 실행 세션은 재시작마다 달라지므로 고정값으로 의존하지 마세요.
- 다음 작업의 외부 선행 조건: 카카오 채널/SENS/승인 템플릿 설정 후 실제 수신 검증. 현재 기본 발송 비활성 상태입니다.
