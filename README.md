# 봉안함 원격 참배 · AI 추모 대화 — MVP (0단계 시연 프로토타입)

여수 지장대사 봉안당을 1차 대상으로 한 "비전스튜디오 프리미엄(가칭)" 시연용 프로토타입입니다.
계획서(`bongan-remote-memorial-plan.md`)의 유족용 6화면 + 관리자용 5화면을 노트북 한 대와 웹캠으로 돌립니다.

- 개발 기획: [docs/DEV_PLAN.md](docs/DEV_PLAN.md)
- 웹캠 선정: [docs/WEBCAM_GUIDE.md](docs/WEBCAM_GUIDE.md)

## 1분 실행

가장 쉬운 방법: 바탕화면의 **봉안당 시연 시작** 바로가기(= 프로젝트 폴더의 `시작.bat`)를 두 번 누릅니다.
서버(8765)와 현장 프로그램(웹캠)이 켜지고 브라우저에 **시작 화면**(`http://127.0.0.1:8765/`)이 열립니다.
시작 화면에는 관리자 콘솔과 시연 계정(이미영·이준호…) 열기 버튼이 있습니다. 이 화면은 서버를 켠 컴퓨터에서 직접 열었을 때만 나오고, 외부·프록시·Vercel에서는 나오지 않습니다(`LAUNCHER=0`으로 끌 수 있음).

명령으로 하려면:

```powershell
pip install -r requirements.txt
python -m server.seed              # 시연 데이터 + 초대 링크 출력
.un.ps1                          # 서버 + 현장 프로그램 실행 후 시작 화면 열기(이미 켜져 있으면 건너뜀)
```

또는 창 두 개에서:

```powershell
python -m uvicorn server.main:app --port 8765 --reload
python -m edge.agent               # 웹캠 캡처 → 사람 감지 → 칸별 사진 업로드
```

| 화면 | 주소 |
|---|---|
| 유족 웹앱 | `http://127.0.0.1:8765/?t=<초대 토큰>` — `python -m server.seed` 가 출력 |
| 관리자 콘솔 | `http://127.0.0.1:8765/admin?key=admin1234` |
| API 문서 | `http://127.0.0.1:8765/docs` |

휴대폰으로 보려면 같은 와이파이에서 `127.0.0.1`을 노트북 IP로 바꾸고 `--host 0.0.0.0`으로 실행합니다.
음성 인식·합성은 크롬·삼성 인터넷·엣지에서 동작합니다(브라우저 내장 기능, 비용 없음).

## 시연 순서 (계획서 3장 서비스 흐름)

1. **관리자 콘솔 → 칸 좌표 등록**: 웹캠 화면 위에 큰 영역을 드래그 → 단·열 입력 → '격자로 나누기' → 저장. 1초 안에 현장 프로그램이 새 칸을 반영합니다.
2. **관리자 콘솔 → 계약·가족**: 계약을 열어 초대 링크를 복사(카카오톡으로 보내는 링크).
3. **유족 앱 → 참배**: 내 칸 사진(10초 갱신). '실시간으로 보기'로 30초 영상. 카메라 앞에 사람이 지나가면 자동으로 사진 화면으로 전환됩니다.
4. **사진을 누르면 → 추모 공간**: 고인 사진·생몰일·방명록·다가오는 기일.
5. **관리자 콘솔 → AI 설정**: Anthropic API 키 입력 → '연결 테스트' → 저장. (없으면 Mock 대화로 시연)
6. **대화**: AI 고지 확인 → 시작. 마이크 버튼으로 말하거나 글로 입력. 재산·의료·법률 질문은 정해진 답으로, 위기 신호("따라가고 싶다")는 고인 역할을 멈추고 109를 안내하며 대화를 닫습니다. 끝나면 요약만 저장됩니다.
7. **제사**: 일정 목록, 진행 중 행사 '중계 보기', 공양·헌화·기도 신청(결제는 모의).
8. **설정**: 가족 초대 링크 발급, **목소리 등록(앱에서 녹음)**, 지난 대화 요약, '작별 절차'(대화 기능 닫기 + 자료 반환/삭제).

## 화면 디자인 · 종교 테마

유족 앱은 어두운 바탕에 금빛 액자·한지 결 질감의 "고급스럽고 신비로운" 디자인입니다. 글자는 18px 이상, 버튼은 58px 이상으로 어르신도 쓰기 쉽게 했습니다.

| 테마 | 분위기 | 고르는 곳 |
|---|---|---|
| 전통(classic) | 먹빛과 금, 한지 결 | 유족 앱 → 설정 → **추모 공간 분위기**(계약자가 고르면 가족 모두 적용, 다른 가족은 자기 기기만) / 관리자 콘솔 → 고인 편집 → **추모 공간 테마** |
| 불교 | 연꽃 문양, 등불의 붉은 빛 | 〃 |
| 천주교 | 성당 창(아치)과 십자가, 푸른 빛 | 〃 |
| 기독교 | 십자가와 새벽 빛줄기 | 〃 |

- **아바타**: 원형이 아니라 **아치형 금빛 액자(창)** 안에 나옵니다. 처음엔 은은한 빛과 안개만 보이다가 4초에 걸쳐 서서히 또렷해지고, 빛줄기·금빛 티끌이 떠다닙니다. 실시간 아바타(D-ID)가 붙으면 사진에서 영상으로 부드럽게 교차됩니다. 말할 때 액자가 은은히 빛나고, 듣는 중엔 푸른 빛으로 바뀝니다.
- **실시간 참배 연출**: "실시간으로 뵙기"를 누르면 범종 소리(합성, 끌 수 있음)와 함께 막이 좌우로 열리고, 영상이 안개 속에서 또렷해지며 양쪽 촛불과 향 연기가 켜지고 "○○○ 님을 뵙습니다"가 떠올랐다 사라집니다. 30초 동안 아주 천천히 다가가는(Ken Burns) 움직임이 붙습니다.
- 애니메이션은 시스템의 "동작 줄이기" 설정을 따릅니다.

## 음성을 더 자연스럽게 (봇 느낌 줄이기)

관리자 콘솔 → AI 설정 → **음성 세부 설정**에서 안정감·유사도·표현력·말 빠르기를 슬라이더로 바꾸고 **저장 전에 미리 듣기**로 비교할 수 있습니다. 효과가 큰 순서:

1. **녹음 원본이 90%** — 또박또박 읽은 목소리는 봇처럼 나옵니다. 조용한 방에서 평소 대화하듯 웃고 쉬어 가며 **3분 이상**(여러 파일 총 5분 안팎) 녹음해 다시 등록하세요.
2. 슬라이더 — 안정감 0.35~0.45, 표현력 0.3~0.45, 속도 0.9~0.95(어르신 말투). 기본값이 이 범위입니다.
3. 모델 — `eleven_v3`가 감정·억양이 가장 자연스럽지만 답이 2~4초 늦습니다(안정감은 0/0.5/1 세 단계로만 적용, 속도 미적용). 실시간이 필요하면 `eleven_v3_conversational`.
4. **Professional Voice Clone** — Creator 요금제(월 $11~)부터, 깨끗한 음성 30분 이상(3시간 최적)이면 원본과 거의 구분되지 않습니다.
5. 다른 API(2026-09-19 조사) — **Fish Audio S2.1 Pro**(한·중·일에 강함, TTS-Arena 상위, 짧은 샘플 복제, 저렴), **Cartesia Sonic 3.6**(한국어, 40~90ms 초저지연 → 실시간 아바타용), **MiniMax Speech-02**(다국어·저비용). 국내 **Supertone**은 검색 결과에 "2026-08-31 API 종료" 공지가 보였으나 문서 페이지가 열리지 않아 직접 확인하지 못했습니다. 어느 것이든 `server/ai/tts.py`에 공급자 클래스 하나를 더하면 붙습니다. 결정은 같은 문장으로 비교 청취한 뒤 하기를 권합니다.

## 음성·아바타 시연 경로

| 등급 | 지금 상태 | 켜는 방법 | 비용 |
|---|---|---|---|
| 기본 AI 음성 | ● 동작 | 아무 설정 없음. **엣지 브라우저**로 열면 신경망 한국어 음성(SunHi·InJoon)이라 품질이 가장 좋음. 호칭(어머니/아버지)에 따라 여성·남성 음성 자동 선택 | 0원 |
| 복제 음성(B등급) | ● 동작 (ElevenLabs 키 입력됨) | **유족 앱 → 설정 → 목소리 등록**: 계약자가 앱에서 1분 녹음(생전 기록) 또는 고인 음성 파일 업로드 → 동의 체크 → 등록 → 미리 듣기. 관리자 콘솔(고인 편집)에서도 같은 일을 할 수 있음 | Starter 월 $6(30,000크레딧 ≈ 10분 대화 25회) |
| 사진 아바타(B등급) | ● 동작 | 대화 화면의 사진이 숨쉬듯 움직이고, 말할 때 미세 움직임·빛·음성 파형이 함께 나옴. 립싱크는 아님 | 0원 |
| 실시간 립싱크 아바타(A등급) | ● 동작 (Simli, 2026-09-19 실제 연결 확인) | 관리자 콘솔 → AI 설정 → Simli 키 → **유족 앱 → 설정 → 얼굴 등록**: 무료 플랜은 **기본 얼굴**(노년 여성·남성 등 7종) 선택, 사진으로 만든 얼굴은 **Simli 유료 플랜** 필요 → 대화 화면에서 사진 대신 입을 맞춘 영상. **복제 음성(ElevenLabs)이 있어야** 켜지고, 연결이 안 되면 사진 아바타로 자동 복귀 | 무료 $10 + 월 50분, 이후 분당 $0.01 미만(Trinity, 2026-09 사이트 안내) |
| 영상 메시지(C등급) | ◐ 재생만 | 외부에서 만든 mp4를 고인 편집 → 자료 올리기(영상 메시지)로 등록하면 추모 공간에 'AI 제작' 표시와 함께 재생 | 편당 1,500~6,000원 |

복제 음성은 약관상 **본인 동의**가 전제입니다. 시연은 생전 기록 자원자(본인 목소리 1~2분 녹음)로 하고, 고인 적용은 법률 자문 뒤에 합니다.
### ElevenLabs API 키 설정 (편집창 항목별 선택값)

키를 만들 때 **키 제한 토글을 끄면** 아래 표는 볼 필요가 없습니다. 제한을 켠다면 그대로 따릅니다. 켜는 건 **텍스트 음성 변환=접근, 음성=작성, 사용자=접근** 세 개뿐이고 나머지는 전부 접근 불가입니다.

| 구분 | 항목 | 선택 |
|---|---|---|
| 기본 | 이름 / 만료 기간 / 사용 제한(크레딧) | 아무거나 / 90일 이상 / 비움 또는 10000 |
| 엔드포인트 | **텍스트 음성 변환** | **접근** |
| 엔드포인트 | 음성 변환, 음성 텍스트 변환, 효과음, 오디오 아이솔레이션, 뮤직 생성, 이미지 및 동영상 생성, 더빙, ElevenAgents, 프로젝트, 오디오 네이티브 | 접근 불가 |
| 엔드포인트 | **음성** (읽음/작성) | **작성** |
| 엔드포인트 | 음성 생성, 강제 정렬, Ads 엔진 | 접근 불가 |
| 관리 | 기록, 모델들, 발음 사전 | 접근 불가 |
| 관리 | **사용자** | **접근** |
| 관리 | 워크스페이스, 워크스페이스 분석, 웹훅, 서비스 계정, 그룹 멤버, 워크스페이스 멤버 읽기/초대/제거, 서비스 약관 동의 | 접근 불가 |
| 그 밖 | IP 주소로 제한 | 비움 |
| 그 밖 | 유출 시 자동 비활성화 | 켬(기본값) |

키는 **생성 직후 한 번만 보이는 `sk_…` 전체 값**을 복사해 관리자 콘솔 → AI 설정 → ElevenLabs 키에 저장합니다. 키 목록의 짧은 요약본은 쓸 수 없고, 옛 키를 지우면 콘솔의 키도 새 것으로 바꿔야 합니다. 같은 표가 관리자 콘솔 AI 설정 화면 안에도 접혀 있습니다.

앱 안 마이크 녹음은 브라우저 보안 규칙상 **https 또는 노트북(localhost)**에서만 됩니다. 휴대폰을 같은 와이파이의 http 주소로 열면 녹음 버튼 대신 '파일로 올리기'만 나오니, 휴대폰 녹음 앱으로 녹음한 파일을 올리거나 https(ngrok 등)로 여세요. 15초 미만 녹음은 거부하고, webm/m4a는 ffmpeg로 mp3로 바꿔 보냅니다.

## 구성

```
server/            FastAPI 서버 + 정적 웹앱
  main.py          앱 진입점 (/, /admin, /api/*)
  db.py            SQLite 스키마·접근
  state.py         라이브 프레임·카메라 상태(메모리, 저장 안 함)
  deps.py          인증(관리자 키·현장 키·가족 초대 토큰)
  routers/edge.py     현장 → 서버 업로드, 라이브 요청 목록
  routers/family.py   원격 참배·추모 공간·제사·공양·초대·작별·목소리 등록
  voice.py         복제 음성 등록 공통 로직(자료 저장·mp3 변환·동의 확인·공급자 등록)
  routers/chat.py     기념일 대화(입장 확인 → 안전 점검 → 응답 → 요약)
  routers/admin.py    칸 좌표·계약·고인·동의서·의례·이용 현황·AI 설정(API 키)
  ai/safety.py     위기·재산·의료·법률 필터, 판매 문구 제거
  ai/prompt.py     고인 말투 시스템 프롬프트(기억 카드 밖 사실 금지)
  ai/llm_anthropic.py  Claude 공급자 (ANTHROPIC_API_KEY 있을 때)
  ai/llm_mock.py   규칙 기반 대화 (키 없을 때 자동)
  ai/tts.py        음성 합성: 브라우저 기본 + ElevenLabs 복제 음성(Instant Voice Clone)
  ai/avatar.py     실시간 아바타: Simli (사진 1장 → 얼굴, 세션 토큰, ICE)
  face.py          얼굴 등록 공통 로직(대표 사진 → 공급자 → deceased.face_id)
  static/          유족 앱(index.html, js/app.js) · 관리자 콘솔(admin.html, js/admin.js)
  seed.py          시연 데이터
edge/agent.py      현장 프로그램: 캡처 → 사람 감지 → 칸 잘라내기·흐림 → 업로드
edge/detector.py   움직임(MOG2) 기본 + HOG/YuNet 선택
tests/test_mvp.py  29개 테스트: python -m pytest -q
```

## 설정 (.env)

| 키 | 기본 | 설명 |
|---|---|---|
| `ADMIN_KEY` / `EDGE_KEY` | admin1234 / edge1234 | 시연 뒤 반드시 변경 |
| `ANTHROPIC_API_KEY` | (없음) | **관리자 콘솔 → AI 설정에서 입력하는 것을 권장.** 콘솔 입력값이 이 값보다 우선. 둘 다 없으면 Mock |
| `LLM_PROVIDER` | auto | auto / anthropic / mock (콘솔에서도 변경 가능) |
| `LIVE_SECONDS` | 30 | 실시간 보기 1회 길이 |
| `CHAT_MAX_MINUTES` | 15 | 대화 1회 제한 |
| `SNAPSHOT_INTERVAL` | 10 | 사진 갱신 주기(초) |
| `CAMERA_INDEX` | 0 | 웹캠 장치 번호 |
| `CAPTURE_WIDTH/HEIGHT` | 1920/1080 | 4K 웹캠이면 3840/2160 |
| `EDGE_PREVIEW` | 0 | 1이면 현장 프로그램이 미리보기 창을 띄움(칸·감지 상태 확인) |
| `AUTO_SEED` / `SEED_SALT` | 0 / (없음) | 서버리스용: 시작 시 시연 데이터 생성, 초대 토큰 고정 |
| `DATA_DIR` | ./data | 쓰기 불가면 자동으로 /tmp 로 내려감(서버리스) |

## 배포

| 방식 | 되는 것 | 안 되는 것 | 언제 |
|---|---|---|---|
| **노트북 + https 터널** (`cloudflared tunnel --url http://localhost:8765`) | 전부. 휴대폰에서 마이크 녹음·음성 인식까지 | — | **시연에 권장.** 웹캠이 노트북에 있으므로 서버도 같은 곳이 가장 단순 |
| Railway / Fly.io / 작은 VM (상시 컨테이너 + 볼륨) | 전부(현장 프로그램은 봉안당 PC에서 `SERVER_URL`을 이 주소로) | — | 파일럿 |
| **Vercel** (서버리스) | 유족 앱·관리자 콘솔 화면, AI 대화, 제사·공양·초대 | 카메라 사진·실시간 보기(인스턴스마다 저장소가 달라 현장 업로드가 안 보임), 데이터 영구 저장(재시작 시 초기화), 60초 넘는 음성 등록 | 화면만 보여 주는 링크 |

Vercel에 올릴 때 환경변수: `AUTO_SEED=1`(시작 시 시연 데이터), `SEED_SALT=아무문자열`(초대 링크 고정), `ADMIN_KEY`, `EDGE_KEY`. API 키는 배포 뒤 관리자 콘솔에서 넣되, 서버리스는 재시작 시 사라지므로 `ANTHROPIC_API_KEY`·`ELEVENLABS_API_KEY`를 환경변수로 넣는 편이 낫습니다. 진입점은 `pyproject.toml`의 `[tool.vercel] entrypoint`.

## 현장 프로그램이 죽었을 때

관리자 콘솔 현황에서 카메라가 "끊김"이면 현장 프로그램이 꺼진 것입니다(유족 앱의 실시간 보기가 "카메라 연결 안 됨"으로 나옴). 프로젝트 폴더에서 다시 실행하세요:

```powershell
python -m edge.agent
```

`run.ps1`(= `시작.bat` = 바탕화면 바로가기)은 서버와 현장 프로그램 중 꺼진 것만 골라 최소화된 창으로 띄운 뒤 시작 화면을 엽니다. 현장 프로그램은 서버가 잠시 내려가도(자동 재시작 등) 스스로 재접속합니다.

## 설계에서 지킨 것 (계획서 4·5·7장)

- **원본 영상은 서버·휴대폰에 가지 않습니다.** 현장 프로그램이 내 칸만 잘라내고 바깥을 흐린 뒤 보냅니다. 관리자 좌표 등록용 전체 화면만 예외이며 열람은 기록됩니다.
- **사람이 보이면 송출을 멈춥니다.** 사진 갱신·실시간 영상 모두 중단되고 유족 화면은 마지막 사진으로 바뀝니다. 영상은 저장하지 않습니다.
- **현장에서 밖으로 나가는 연결만** 씁니다. 서버가 카메라로 들어가는 길이 없습니다.
- **AI 표시 상시 노출**, 아바타는 아무것도 팔지 않음(응답에서 권유 문장 제거), 15분 제한, 요약만 저장, 동의서 없으면 대화 불가, 미성년자 보호자 확인, 작별 절차.
- LLM·TTS는 인터페이스로 분리되어 공급자 교체가 가능합니다(`server/ai/base.py`).

## 다음 단계 (파일럿 전)

- 4K 웹캠 현장 시험: 반사·조도·색 (docs/WEBCAM_GUIDE.md 체크리스트)
- 복제 음성: ElevenLabs 연동은 되어 있음. 수퍼톤(국내) 공급자 추가와 약관상 동의 요건 서면 확인
- 실시간 아바타(A등급): Simli 연동은 되어 있음(SDK 없이 WebRTC 직접 구현, `app.js`의 `LiveAvatar`). 실제 얼굴 품질·지연·약관(실제 인물 사진) 확인
- 카카오 로그인·휴대폰 본인확인, 알림톡, 국내 PG 정기결제
- PostgreSQL 전환, WebRTC 실시간 영상, PoE IP 카메라(RTSP) 입력
- 법률 자문 확정(카메라 설치 목적, 동의서 문안)
